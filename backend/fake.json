declare global {
    namespace Express {
        interface Request {
            currentUser: any
    }
  }
}

BaseRepository<T extends IBase, Document>

ở ngoài thì có thể viết thành
class CustomerRepository extends BaseRepository<ICustomer>